package com.bekisma.adlamfulfulde.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Palette de couleurs inspirée de la culture peule/Fulani
 * Basée sur les couleurs traditionnelles: ocre, rouge terre, blanc, bleu indigo, or/jaune
 * Optimisée pour l'accessibilité WCAG 2.1 AA et Material Design 3
 */

// === COULEURS TRADITIONNELLES PEULES (ACCESSIBILITÉ AMÉLIORÉE) ===

// Ocre (couleur de la terre, des poteries traditionnelles)
val PeulOcre = Color(0xFFB8763E)          // Ocre principal - contrast amélioré
val PeulOcreLight = Color(0xFFE6B584)     // Ocre clair
val PeulOcreDark = Color(0xFF8B5A2B)      // Ocre foncé - plus sombre pour meilleur contraste
val PeulOcreContainer = Color(0xFFF5E6D3) // Container ocre très clair

// Rouge terre (couleur des sols latéritiques d'Afrique de l'Ouest)
val PeulRedEarth = Color(0xFFA54A2E)      // Rouge terre principal - contrast amélioré
val PeulRedEarthLight = Color(0xFFD4896F) // Rouge terre clair
val PeulRedEarthDark = Color(0xFF7A321A)  // Rouge terre foncé - plus sombre
val PeulRedContainer = Color(0xFFF0E0DB)  // Container rouge très clair

// Blanc (pureté, sagesse des anciens)
val PeulWhite = Color(0xFFFFFBF7)         // Blanc cassé chaleureux
val PeulWhiteIvory = Color(0xFFF7F3ED)    // Blanc ivoire
val PeulWhiteSoft = Color(0xFFFAF7F2)     // Blanc doux

// Bleu indigo (teintures traditionnelles, sagesse)
val PeulIndigo = Color(0xFF1E4A5F)        // Indigo principal - contrast amélioré
val PeulIndigoLight = Color(0xFF6B8FA3)   // Indigo clair
val PeulIndigoDark = Color(0xFF0F2633)    // Indigo foncé - plus sombre
val PeulIndigoContainer = Color(0xFFE8F1F5) // Container indigo très clair

// Or/Jaune (richesse, prospérité, bijoux traditionnels)
val PeulGold = Color(0xFFD1A500)          // Or principal - contrast amélioré
val PeulGoldLight = Color(0xFFF2D64D)     // Or clair
val PeulGoldDark = Color(0xFF9C7800)      // Or foncé - plus sombre
val PeulGoldContainer = Color(0xFFFAF5E6) // Container or très clair

// === COULEURS COMPLEMENTAIRES ===

// Vert (nature, pâturages, mode de vie pastoral)
val PeulGreen = Color(0xFF5C8A3A)         // Vert prairie
val PeulGreenLight = Color(0xFF8DB66B)    // Vert clair
val PeulGreenDark = Color(0xFF3E5C26)     // Vert foncé
val PeulGreenContainer = Color(0xFFEDF3E7) // Container vert très clair

// Marron (cuir, artisanat traditionnel)
val PeulBrown = Color(0xFF8B4513)         // Marron cuir
val PeulBrownLight = Color(0xFFB87556)    // Marron clair
val PeulBrownDark = Color(0xFF5D2F0C)     // Marron foncé
val PeulBrownContainer = Color(0xFFF2E8E2) // Container marron très clair

// === COULEURS NEUTRES CULTURELLES ===

val PeulNeutralLight = Color(0xFFF5F2EE)  // Neutre clair
val PeulNeutralMedium = Color(0xFFE0D7D0) // Neutre moyen
val PeulNeutralDark = Color(0xFF6B5D52)   // Neutre foncé
val PeulNeutralCharcoal = Color(0xFF3A332B) // Charbon

// === COULEURS D'ACCENT ===

val PeulAccentWarm = Color(0xFFD4A574)    // Accent chaleureux
val PeulAccentCool = Color(0xFF7BA3C0)    // Accent frais
val PeulAccentVibrant = Color(0xFFE08B47) // Accent vibrant

// === COULEURS DE STATUT ===

val PeulSuccess = Color(0xFF6B8A3F)       // Succès (vert nature)
val PeulWarning = Color(0xFFE6B800)       // Attention (or)
val PeulError = Color(0xFFB85C3E)         // Erreur (rouge terre)
val PeulInfo = Color(0xFF2C5F7C)          // Information (indigo)

// === COULEURS DE SURFACE ===

val PeulSurfaceLight = Color(0xFFFAF7F2)  // Surface claire
val PeulSurfaceMedium = Color(0xFFF0ECE5) // Surface moyenne
val PeulSurfaceDark = Color(0xFF1A1612)   // Surface sombre
val PeulSurfaceElevated = Color(0xFFFFFBF7) // Surface élevée

// === COULEURS MATERIAL 3 SUPPLÉMENTAIRES ===

// Outline colors for better visual hierarchy
val PeulOutline = Color(0xFF79716B)
val PeulOutlineVariant = Color(0xFFCAC4BD)

// Inverse colors for high contrast elements
val PeulInverseSurface = Color(0xFF322F2B)
val PeulInverseOnSurface = Color(0xFFF2EFE9)
val PeulInversePrimary = Color(0xFFE6B584)

// Scrim for overlays
val PeulScrim = Color(0xFF000000)

// Shadow colors
val PeulShadow = Color(0xFF000000)

// === COULEURS SÉMANTIQUES POUR L'APPRENTISSAGE ===

// Feedback colors for learning app
val PeulCorrect = Color(0xFF2E7D32)       // Vert pour réponses correctes
val PeulIncorrect = Color(0xFFD32F2F)     // Rouge pour erreurs
val PeulProgress = Color(0xFF1976D2)      // Bleu pour progression
val PeulHint = Color(0xFFF57C00)          // Orange pour indices

// Special colors for Adlam script display
val PeulAdlamPrimary = Color(0xFF2C5F7C)  // Couleur principale pour l'écriture Adlam
val PeulAdlamAccent = Color(0xFFE6B800)   // Couleur d'accent pour l'écriture Adlam